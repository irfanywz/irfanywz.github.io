---
title: "File Tree Generator"
date: 2026-06-06T10:00:00+07:00
description: "Alat untuk menghasilkan struktur direktori atau file tree secara visual dan rapi secara real-time."
icon: "icon-[ri--folder-tree-line]"
---

<div class="max-w-4xl mx-auto mt-6 space-y-6"> <div class="flex flex-col gap-2"> <label for="input" class="font-bold text-gray-700 dark:text-gray-300 flex items-center gap-2"> <i class="icon-[ri--input-method-line]"></i> Daftar File / Path (Satu per baris) </label> <textarea id="input" rows="8" placeholder="src/components/Button.jsx&#10;src/index.js&#10;package.json&#10;README.md" class="w-full p-4 rounded-xl border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 text-gray-900 dark:text-white focus:ring-2 focus:ring-indigo-500 outline-none transition-colors resize-y shadow-sm font-mono text-sm"></textarea> </div> <div class="flex flex-wrap gap-3 justify-center sm:justify-start"> <button id="clearBtn" class="px-6 py-2.5 bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400 font-medium rounded-lg hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors active:scale-95 flex items-center gap-2 border border-gray-200 dark:border-gray-700 cursor-pointer"> <i class="icon-[ri--delete-bin-line]"></i> Bersihkan </button> </div> <div class="flex flex-wrap items-center justify-between gap-4 p-4 rounded-xl border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-800/50"> <label class="flex items-center gap-2 cursor-pointer text-gray-700 dark:text-gray-300 select-none"> <input type="checkbox" id="wrapPreCode" class="w-4 h-4 accent-indigo-500 rounded cursor-pointer"> <span class="text-sm font-medium">Bungkus dengan <code class="text-indigo-600 dark:text-indigo-400">&lt;pre&gt;&lt;code&gt;</code></span> </label> </div> <div class="flex flex-col gap-2"> <div class="flex justify-between items-center"> <label for="output" class="font-bold text-gray-700 dark:text-gray-300 flex items-center gap-2"> <i class="icon-[ri--terminal-box-line]"></i> Hasil Struktur Direktori </label> <button id="copyBtn" class="text-xs px-3 py-1.5 bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400 rounded-lg hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors flex items-center gap-1.5 border border-gray-200 dark:border-gray-700 cursor-pointer"> <i class="icon-[ri--file-copy-line]"></i> Salin </button> </div> <textarea id="output" rows="8" readonly class="w-full p-4 rounded-xl border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-white font-mono text-sm focus:ring-2 focus:ring-indigo-500 outline-none transition-colors resize-y shadow-inner" placeholder="Struktur tree akan muncul di sini..."></textarea> </div> </div> <script> (function() { const input = document.getElementById('input'); const output = document.getElementById('output'); const clearBtn = document.getElementById('clearBtn'); const copyBtn = document.getElementById('copyBtn'); const wrapPreCode = document.getElementById('wrapPreCode'); function buildTree(paths) { let root = {}; paths.forEach(path => { let parts = path.split('/').filter(Boolean); let current = root; parts.forEach(part => { if (!current[part]) { current[part] = {}; } current = current[part]; }); }); return root; } function renderTree(node, prefix = '') { let result = ''; let keys = Object.keys(node).sort((a, b) => { let aIsFolder = Object.keys(node[a]).length > 0; let bIsFolder = Object.keys(node[b]).length > 0; if (aIsFolder && !bIsFolder) return -1; if (!aIsFolder && bIsFolder) return 1; return a.localeCompare(b); }); keys.forEach((key, index) => { let isLast = index === keys.length - 1; let pointer = isLast ? '└── ' : '├── '; result += prefix + pointer + key + '\n'; let children = node[key]; if (Object.keys(children).length > 0) { let extension = isLast ? ' ' : '│ '; result += renderTree(children, prefix + extension); } }); return result; } function updateTree() { let lines = input.value.split('\n').map(l => l.trim()).filter(Boolean); if (lines.length === 0) { output.value = ''; return; } let treeObj = buildTree(lines); let treeStr = renderTree(treeObj).trim(); if (wrapPreCode.checked) { output.value = `<pre><code class="language-text">\n${treeStr}\n</code></pre>`; } else { output.value = treeStr; } } input.addEventListener('input', updateTree); wrapPreCode.addEventListener('change', updateTree); clearBtn.addEventListener('click', () => { input.value = ''; output.value = ''; wrapPreCode.checked = false; input.focus(); }); copyBtn.addEventListener('click', () => { if (!output.value) return; navigator.clipboard.writeText(output.value).then(() => { const originalHTML = copyBtn.innerHTML; copyBtn.innerHTML = '<i class="icon-[ri--check-line]"></i> Tersalin'; copyBtn.classList.add('text-emerald-600', 'dark:text-emerald-400'); setTimeout(() => { copyBtn.innerHTML = originalHTML; copyBtn.classList.remove('text-emerald-600', 'dark:text-emerald-400'); }, 2000); }); }); })(); </script>